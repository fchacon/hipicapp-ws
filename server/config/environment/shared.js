/**
 * Shared Environments Variables
 */

'use strict';

const vars = {
    
    session: {
        secret: 'shhhhhhared-secret'
    },

    userRoles: ['guest', 'user', 'admin']
};

module.exports = vars;